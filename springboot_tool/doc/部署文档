1. 使用IDE导入本项目
2. 下载redis 启动redis
(不需要做这一步) 3. 创建数据库, 导入***.sql
3. 创建数据库, 我自己的SQL脚本.sql即可
4. 配置application-dev.yml中的redis以及数据库连接
5. 运行项目
   1、直接运行 SpringbootToolApplication.java
   （不需要做此步）2、项目根目录下执行mvn -X clean package -Dmaven.test.skip=true编译打包，然后执行java -jar manager.jar
6. 接口文档访问 http://localhost:11112/doc.html
